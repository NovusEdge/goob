# Reference

> Placeholder: technical reference for extending goob.

For the practical `PetConfig` field-by-field reference, see
[Configuration](configuration.md). This page is for deeper extension details,
still to be written:

## Planned

- `PetConfig` resource schema (types + defaults)
- Engine-verb → animation resolution and the fallback chain
- State machine API (`PetBrain`)
- Mood system hooks
- Daemon `/tick` contract and the `emit` tool
