# Godot 4 import cache — regenerated on open
.godot/

# Godot export artifacts
*.exe
export_presets.cfg

# Local planning docs (specs, implementation plans) — not part of the project
docs/superpowers/

# Agent scratch + local Claude Code guide — not part of the project
.claude/
CLAUDE.md

# LimboAI sample project — the addon itself (addons/limboai/) is committed
/demo/

# Local env (DEBUG toggle etc.)
.env

# Python bytecode
__pycache__/
*.pyc

# uv virtualenv
.venv/

# TUI build artifact (go run leaves it; `just tui` rebuilds)
tui/tui
installer/installer
